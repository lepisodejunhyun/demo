/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageInfoDto } from '../../models/page-info-dto';
import { PreRegistrationDto } from '../../models/pre-registration-dto';

export interface PreRegistrationControllerFindAll$Params {

/**
 * 페이지 번호
 */
  page?: number;

/**
 * 페이지당 항목 수
 */
  limit?: number;
}

export function preRegistrationControllerFindAll(http: HttpClient, rootUrl: string, params?: PreRegistrationControllerFindAll$Params, context?: HttpContext): Observable<StrictHttpResponse<{
'items'?: Array<PreRegistrationDto>;
'pageInfo'?: PageInfoDto;
}>> {
  const rb = new RequestBuilder(rootUrl, preRegistrationControllerFindAll.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      'items'?: Array<PreRegistrationDto>;
      'pageInfo'?: PageInfoDto;
      }>;
    })
  );
}

preRegistrationControllerFindAll.PATH = '/api/pre-registration';
